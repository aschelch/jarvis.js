/**
 * Module dependencies.
 */


/**
 * Motion middleware
 *
 * Examples:
 *
 *       .use(motion())
 *       .use(function(bot){
 *          bot.on('motion', function(){
 *              console.log();
 *          });
 *       });
 *
 */

module.exports = function music(options){
    options = options || {};

    return function(bot){
    };
}
