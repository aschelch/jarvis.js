/**
 * Module dependencies.
 */

/**
 * Voice recognition
 *
 * Examples:
 *
 *       .use(voice-recognition())
 *       .use(function(bot){
 *
 *       });
 *
 */

module.exports = function recognition(options){
    options = options || {};

    return function recognition(bot){

        // TODO

    };
}
